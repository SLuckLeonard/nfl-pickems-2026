// Phase 4: Single game pick card, reused in both pick sheets.
// Shows away team vs home team as two clickable buttons.
// Visual states: default | selected | correct (green) | wrong (red) | locked

export default function GameCard() {
  // Placeholder — full implementation in Phase 4.
  return null;
}
